[project]
name = "podman-agents"
version = "0.1.0"
description = "Podman agents for container management"
requires-python = ">=3.10"
dependencies = [
    "acp-sdk",
    "fastapi>=0.109.0",
    "uvicorn>=0.27.0",
    "pydantic>=2.6.0",
    "python-dotenv>=1.0.0",
    "podman-compose>=1.0.3",
] 